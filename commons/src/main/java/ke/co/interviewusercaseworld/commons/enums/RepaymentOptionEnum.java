package ke.co.interviewusercaseworld.commons.enums;

public enum RepaymentOptionEnum {
    INSTALLMENT,     // Paid in periodic installments (EMI)
    BULLET           // Principal + interest paid at once at maturity
}
